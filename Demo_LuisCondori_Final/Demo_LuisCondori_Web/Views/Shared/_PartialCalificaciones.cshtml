@model IEnumerable<Demo_LuisCondori.Entidad.Entidad.Calificacion>

@{
    ViewBag.Title = "View";
}

<h2>Notas</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CodigoAlumno)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreEvaluacion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nota)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoAlumno)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreEvaluacion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nota)
        </td>
        <!--<td>
        @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
        @Html.ActionLink("Details", "Details", new { id=item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id=item.Id })
    </td>-->
    </tr>
}

</table>
